from rest_framework import routers

# from tool.api.viewsets import (JobViewSet, BackgroundTypeViewSet, BackgroundUploadViewSet,
# 							DataUploadViewSet, ProteomeUploadViewSet)

# router = routers.DefaultRouter()

# router.register('jobs', JobViewSet, base_name='job')
# router.register('backgroundtypes', BackgroundTypeViewSet)
# router.register('backgrounduploads', BackgroundUploadViewSet)
# router.register('datauploads', DataUploadViewSet)
# router.register('proteomeuploads', ProteomeUploadViewSet)